{
  "build": {
    "builder": "php",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm install && npx vite build && cd ../frontend && npm install && npx vite build && cd ../backend"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8000",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}